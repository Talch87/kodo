# Goal: Build a CLI Todo App

Create a simple command-line todo application in Python.

## Requirements
- Add, list, complete, and delete tasks
- Persist tasks to a JSON file
- Clean CLI interface using argparse
- Include basic tests with pytest
