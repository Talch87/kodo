[project]
name = "selfocode"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = [
    "claude-agent-sdk",
    "python-dotenv>=1.0",
    "simple-term-menu",
    "pydantic-ai[anthropic,google]",
]

[project.scripts]
selfocode = "selfocode.cli:main"
