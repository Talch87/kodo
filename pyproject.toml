[project]
name = "selfocode"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = [
    "anthropic>=0.79.0",
    "claude-agent-sdk",
    "python-dotenv>=1.0",
    "simple-term-menu",
]

[project.scripts]
selfocode = "selfocode.cli:main"
